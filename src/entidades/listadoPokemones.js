export default class ListadoPokemones {
  constructor(cantidad, urlSiguiente, urlAnterior, pokemones) {
    this.cantidad = cantidad;
    this.urlSiguiente = urlSiguiente;
    this.urlAnterior = urlAnterior;
    this.pokemones = pokemones;
  }
}
